@model IEnumerable<DevHub.Models.JobPost>
@{
    ViewData["Title"] = "Tìm Việc Làm";
}

<!-- Top Header Section -->
<section class="py-12 border-b border-bordercolor relative overflow-hidden" style="background-color: #F7F5FF; background-image: radial-gradient(circle at 75% 35%, rgba(139, 92, 246, 0.18), transparent 35%);">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <!-- Breadcrumb -->
        <div class="text-sm text-muted mb-4 flex items-center space-x-2">
            <svg class="w-4 h-4 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
            </svg>
            <a href="/" class="hover:text-primary transition-colors">Trang chủ</a>
            <span>&rsaquo;</span>
            <a href="/Jobs" class="hover:text-primary transition-colors">Việc làm</a>
            <span>&rsaquo;</span>
            <span class="text-dark font-medium">Tất cả công việc</span>
        </div>

        <!-- Title -->
        <h1 id="jobs-title" class="text-[32px] md:text-[40px] font-bold text-dark mb-8">
            @if (!string.IsNullOrEmpty(ViewBag.Keyword))
            {
                <text>Kết quả <span class="text-[#26A4FF]">@ViewBag.TotalJobs việc làm @ViewBag.Keyword</span></text>
            }
            else
            {
                <text>Tất cả các <span class="text-[#26A4FF]">việc làm</span> <span class="text-muted text-[20px] font-normal">(Tất cả @ViewBag.TotalJobs công việc)</span></text>
            }
        </h1>

        <!-- Search Bar -->
        <form action="/Jobs" method="GET" class="bg-white p-3 shadow-[0_15px_40px_-10px_rgba(139,92,246,0.3)] hover:shadow-[0_20px_50px_-15px_rgba(139,92,246,0.4)] transition-shadow duration-300 md:rounded-full rounded-3xl flex flex-col md:flex-row items-center border border-purple-100 relative z-20">
            <div class="flex items-center flex-1 w-full px-4 py-2 border-b md:border-b-0 md:border-r border-bordercolor mb-2 md:mb-0">
                <svg class="w-5 h-5 text-gray-400 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
                <input id="jobs-keyword" name="keyword" type="text" placeholder="Job title or keyword" value="@ViewBag.Keyword" class="w-full focus:outline-none text-dark placeholder-gray-400">
            </div>
            <div class="flex items-center flex-1 w-full px-4 py-2 mb-2 md:mb-0 relative">
                <svg class="w-5 h-5 text-gray-400 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                </svg>
                <select name="location" class="w-full focus:outline-none text-dark appearance-none bg-transparent cursor-pointer">
                    <option value="" selected="@(string.IsNullOrEmpty(ViewBag.Location) ? "selected" : null)">Tất cả địa điểm</option>
                    <option value="Hà Nội" selected="@(ViewBag.Location == "Hà Nội" ? "selected" : null)">TP Hà Nội</option>
                    <option value="Hồ Chí Minh" selected="@(ViewBag.Location == "Hồ Chí Minh" ? "selected" : null)">Hồ Chí Minh</option>
                    <option value="Đà Nẵng" selected="@(ViewBag.Location == "Đà Nẵng" ? "selected" : null)">Đà Nẵng</option>
                </select>
            </div>
            <button type="submit" id="jobs-search-btn" class="w-full md:w-auto bg-primary text-white font-bold py-3.5 px-8 md:rounded-full rounded-[20px] hover:bg-opacity-90 transition-all whitespace-nowrap text-[16px] flex-shrink-0 shadow-md">
                Tìm kiếm việc làm
            </button>
        </form>

        <!-- Popular Tags -->
        <div class="mt-4 text-[14px] text-muted flex items-center flex-wrap">
            <span class="mr-2">Phổ biến :</span>
            <a href="#" class="hover:text-primary transition-colors mr-2">UI Designer,</a>
            <a href="#" class="hover:text-primary transition-colors mr-2">UX Researcher,</a>
            <a href="#" class="hover:text-primary transition-colors mr-2">Android,</a>
            <a href="#" class="hover:text-primary transition-colors">Admin</a>
        </div>
    </div>
</section>

<!-- Main Content -->
<section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 flex flex-col md:flex-row gap-8">
    <!-- Sidebar Filters -->
    <div class="w-full md:w-1/4 border border-gray-200 shadow-[0_8px_30px_rgb(0,0,0,0.12)] rounded-[20px] p-5 bg-white h-fit">
        <!-- Filter Category -->
        <div class="mb-5 border-b border-gray-50 pb-5 last:border-0 last:pb-0">
            <div class="flex justify-between items-center mb-3 cursor-pointer select-none filter-header">
                <h3 class="font-bold text-dark text-[15px]">Vị trí làm việc</h3>
                <svg class="w-4 h-4 text-dark transition-transform duration-300 filter-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7" />
                </svg>
            </div>
            <div class="space-y-2.5 filter-content overflow-hidden transition-all duration-300">
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="checkbox" class="form-checkbox h-4 w-4 text-primary rounded border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">Cơ bản</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="checkbox" class="form-checkbox h-4 w-4 text-primary rounded border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">Cơ hội thăng tiến cao</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="checkbox" class="form-checkbox h-4 w-4 text-primary rounded border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">Văn hóa và giá trị</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="checkbox" class="form-checkbox h-4 w-4 text-primary rounded border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">Lãnh đạo cấp cao</span>
                </label>
            </div>
        </div>

        <!-- Filter Category -->
        <div class="mb-5 border-b border-gray-50 pb-5 last:border-0 last:pb-0">
            <div class="flex justify-between items-center mb-3 cursor-pointer select-none filter-header">
                <h3 class="font-bold text-dark text-[15px]">Loại công việc</h3>
                <svg class="w-4 h-4 text-dark transition-transform duration-300 filter-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7" />
                </svg>
            </div>
            <div class="space-y-2.5 filter-content overflow-hidden transition-all duration-300">
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="radio" name="jobType" class="form-radio h-4 w-4 text-primary border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">Full time</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="radio" name="jobType" class="form-radio h-4 w-4 text-primary border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">Part time</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="radio" name="jobType" class="form-radio h-4 w-4 text-primary border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">Other</span>
                </label>
            </div>
        </div>

        <!-- Filter Category -->
        <div class="mb-5 border-b border-gray-50 pb-5 last:border-0 last:pb-0">
            <div class="flex justify-between items-center mb-3 cursor-pointer select-none filter-header">
                <h3 class="font-bold text-dark text-[15px]">Mức lương</h3>
                <svg class="w-4 h-4 text-dark transition-transform duration-300 filter-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7" />
                </svg>
            </div>
            <div class="space-y-2.5 filter-content overflow-hidden transition-all duration-300">
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="radio" name="salary" class="form-radio h-4 w-4 text-primary border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">1 - 50$</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="radio" name="salary" class="form-radio h-4 w-4 text-primary border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">51 - 200$</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="radio" name="salary" class="form-radio h-4 w-4 text-primary border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">201 - 500$</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="radio" name="salary" class="form-radio h-4 w-4 text-primary border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">501 - 1000$</span>
                </label>
                <label class="flex items-center space-x-3 cursor-pointer group">
                    <input type="radio" name="salary" class="form-radio h-4 w-4 text-primary border-gray-300 focus:ring-primary">
                    <span class="text-muted text-[14px] group-hover:text-dark transition-colors">1000$+</span>
                </label>
            </div>
        </div>
    </div>

    <!-- Job List -->
    <div class="w-full md:w-3/4">
        <div class="space-y-4">
            @if (Model != null && Model.Any())
            {
                foreach (var job in Model)
                {
                    <div class="block bg-white border border-gray-200 shadow-[0_8px_30px_rgb(0,0,0,0.12)] hover:border-primary hover:shadow-md transition-all rounded-[20px] p-6 group cursor-pointer" onclick="window.location='/Jobs/Details/@job.JobId'">
                        <div class="flex justify-between items-start mb-4">
                            <div class="flex items-center gap-4">
                                <div class="w-12 h-12 rounded-full flex items-center justify-center overflow-hidden border border-gray-200 flex-shrink-0">
                                    @if (!string.IsNullOrEmpty(job.Recruiter?.CompanyLogoUrl))
                                    {
                                        <img src="@job.Recruiter.CompanyLogoUrl" alt="@job.Recruiter.CompanyName" class="w-full h-full object-cover p-1">
                                    }
                                    else
                                    {
                                        <div class="w-full h-full bg-gray-100 flex items-center justify-center text-primary font-bold">
                                            @(job.Recruiter?.CompanyName?.Substring(0, 1) ?? "C")
                                        </div>
                                    }
                                </div>
                                 <div>
                                    <h3 class="text-[18px] font-bold text-dark group-hover:text-primary transition-colors">@job.Title</h3>
                                    <div class="flex items-center text-muted text-[14px] mt-0.5 gap-2">
                                        <span>@job.Recruiter?.CompanyName</span>
                                        <span>•</span>
                                        <span class="flex items-center gap-1">
                                            <svg class="w-4 h-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                                            </svg>
                                            @job.Location
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <!-- Bookmark button -->
                            <button onclick="event.stopPropagation()" class="text-gray-400 hover:text-primary transition-colors flex-shrink-0 ml-2">
                                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"/>
                                </svg>
                            </button>
                        </div>


                        <p class="text-muted text-[15px] mb-4 line-clamp-2">
                            @Html.Raw(job.Description?.Length > 150 ? job.Description.Substring(0, 150) + "..." : job.Description)
                        </p>

                        <div class="flex items-center gap-2 mb-4">
                            <span class="text-[#26A4FF] font-medium text-[15px] flex items-center">
                                <svg class="w-5 h-5 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                                @(job.SalaryMin != null && job.SalaryMax != null ? $"{job.SalaryMin:N0} - {job.SalaryMax:N0} triệu" : "Thỏa thuận")
                            </span>
                        </div>

                        <div class="flex items-center justify-between flex-wrap gap-3">
                            <div class="flex flex-wrap gap-2">
                                <span class="px-3 py-1 rounded-full border border-green-500 text-green-500 text-[13px] font-medium bg-green-50">@job.WorkingModel</span>
                                <span class="px-3 py-1 rounded-full border border-primary text-primary text-[13px] font-medium bg-primary/5">@job.Position?.PositionName</span>
                            </div>
                            <a href="/Jobs/Details/@job.JobId" onclick="event.stopPropagation()" class="bg-primary text-white text-[13px] font-semibold px-5 py-2 rounded-full hover:bg-opacity-90 transition-all shadow-sm whitespace-nowrap">
                                Ứng tuyển
                            </a>
                        </div>
                    </div>

                }
            }
            else
            {
                <div class="bg-white rounded-[20px] p-8 text-center border border-gray-200">
                    <svg class="w-16 h-16 text-gray-400 mx-auto mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h3 class="text-xl font-bold text-dark mb-2">Không tìm thấy việc làm nào</h3>
                    <p class="text-muted">Rất tiếc, không có công việc nào phù hợp với tìm kiếm của bạn.</p>
                </div>
            }
        </div>

        <!-- Pagination -->
        @if (ViewBag.TotalPages > 1)
        {
            <div class="mt-8 flex justify-center">
                <nav class="flex items-center space-x-2">
                    @if (ViewBag.CurrentPage > 1)
                    {
                        <a href="@Url.Action("Index", new { keyword = ViewBag.Keyword, location = ViewBag.Location, page = ViewBag.CurrentPage - 1 })" class="w-10 h-10 flex items-center justify-center rounded border border-transparent text-muted hover:bg-gray-100 transition-colors">
                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                            </svg>
                        </a>
                    }
                    
                    @for (int p = 1; p <= ViewBag.TotalPages; p++)
                    {
                        if (p == ViewBag.CurrentPage)
                        {
                            <span class="w-10 h-10 flex items-center justify-center rounded border border-primary bg-primary text-white font-medium">@p</span>
                        }
                        else if (p == 1 || p == ViewBag.TotalPages || (p >= ViewBag.CurrentPage - 2 && p <= ViewBag.CurrentPage + 2))
                        {
                            <a href="@Url.Action("Index", new { keyword = ViewBag.Keyword, location = ViewBag.Location, page = p })" class="w-10 h-10 flex items-center justify-center rounded border border-transparent text-dark hover:bg-gray-100 font-medium transition-colors">@p</a>
                        }
                        else if (p == ViewBag.CurrentPage - 3 || p == ViewBag.CurrentPage + 3)
                        {
                            <span class="text-muted px-2">...</span>
                        }
                    }

                    @if (ViewBag.CurrentPage < ViewBag.TotalPages)
                    {
                        <a href="@Url.Action("Index", new { keyword = ViewBag.Keyword, location = ViewBag.Location, page = ViewBag.CurrentPage + 1 })" class="w-10 h-10 flex items-center justify-center rounded border border-transparent text-muted hover:bg-gray-100 transition-colors">
                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                            </svg>
                        </a>
                    }
                </nav>
            </div>
        }
    </div>
</section>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Set initial max-height for open filters so transition works
            const filterContents = document.querySelectorAll('.filter-content');
            filterContents.forEach(content => {
                content.style.maxHeight = content.scrollHeight + "px";
                content.style.opacity = '1';
            });

            // Handle click events
            const headers = document.querySelectorAll('.filter-header');
            headers.forEach(header => {
                header.addEventListener('click', () => {
                    const content = header.nextElementSibling;
                    const icon = header.querySelector('.filter-icon');
                    
                    if (content.style.maxHeight && content.style.maxHeight !== '0px') {
                        // Collapse
                        content.style.maxHeight = '0px';
                        content.style.opacity = '0';
                        icon.classList.add('rotate-180');
                        content.classList.add('mt-0');
                    } else {
                        // Expand
                        content.style.maxHeight = content.scrollHeight + "px";
                        content.style.opacity = '1';
                        icon.classList.remove('rotate-180');
                        content.classList.remove('mt-0');
                    }
                });
            });
        });

    </script>
}
