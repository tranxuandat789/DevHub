<!--AnhPT-03/06/2026-->
@model DevHub.Models.Recruiter
@using System.Security.Claims
@{
    var displayName = Model.FullName;
    //get logo company, if null generate default logo by user name
    var displayAvatar = !string.IsNullOrEmpty(Model.CompanyLogoUrl)
        ? Model.CompanyLogoUrl
        : $"https://ui-avatars.com/api/?name={Uri.EscapeDataString(displayName)}&background=4640DE&color=fff&size=128";
    var email = Model.RecruiterNavigation?.Email ?? User.FindFirstValue(ClaimTypes.Email) ?? "";

    //saved the typed information in form from user in case it got failed by validation
    string FieldValue(string key, string? fallback) =>
        ViewData.ModelState.TryGetValue(key, out var entry) && entry.AttemptedValue != null
            ? entry.AttemptedValue
            : (fallback ?? "");
}
<div>
    <h2 class="text-lg font-medium text-gray-900 mb-6">Thông tin cá nhân</h2>

    <form asp-action="UpdateAccount" asp-controller="Settings" method="post" class="space-y-6">
        @Html.AntiForgeryToken()
        <!-- Avatar (Company logo) -->
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Ảnh đại diện</label>
            <div class="flex items-center gap-6">
                <img src="@displayAvatar" alt="Avatar" class="w-24 h-24 rounded-full object-cover border-2 border-white shadow-sm" id="avatarPreview"
                     onerror="this.src='https://ui-avatars.com/api/?name=@Uri.EscapeDataString(displayName)&background=4640DE&color=fff&size=128'">
                <div>
                    <p class="text-sm text-gray-600">Ảnh đại diện được quản lý trong mục <a href="/Recruiter/Settings?tab=company" class="text-blue-600">Thông tin công ty</a>. Vui lòng cập nhật logo công ty ở đó.</p>
                </div>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!--Full name textbox-->
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Họ và tên</label>
                <input type="text" name="FullName" class="w-full px-4 py-2.5 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 transition-colors" value="@FieldValue("FullName", Model.FullName)" placeholder="Nhập họ và tên">
                @Html.ValidationMessage("FullName", new { @class = "text-xs text-red-500 mt-1 block" })
            </div>
            <!--Phone number textbox-->
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Số điện thoại</label>
                <input type="tel" name="Phone" class="w-full px-4 py-2.5 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 transition-colors" value="@FieldValue("Phone", Model.Phone)" placeholder="Nhập số điện thoại">
                @Html.ValidationMessage("Phone", new { @class = "text-xs text-red-500 mt-1 block" })
            </div>
            <!--Email textbox, readonly -->
            <div class="md:col-span-2">
                <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                <input type="email" name="Email" class="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-lg text-gray-500 cursor-not-allowed" value="@email" disabled>
                <p class="text-xs text-gray-500 mt-1">Email không thể thay đổi vì được dùng làm tên đăng nhập.</p>
            </div>
            <!--The job position of the staff -->
            <div class="md:col-span-2">
                <label class="block text-sm font-medium text-gray-700 mb-1">Chức vụ trong công ty</label>
                <input type="text" name="Position" class="w-full px-4 py-2.5 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 transition-colors" value="@FieldValue("Position", Model.Position)" placeholder="Ví dụ: Giám đốc, Trưởng phòng HR...">
            </div>
        </div>
        <!--Save button -->
        <div class="pt-4 flex justify-end">
            <button type="submit" class="px-6 py-2.5 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition-colors shadow-sm shadow-blue-500/20">
                Lưu thay đổi
            </button>
        </div>
    </form>
</div>
<!--script to preview the logo-->
<script>
    function previewImage(input, previewId) {
        if (input.files && input.files[0]) {
            var reader = new FileReader();
            reader.onload = function(e) {
                document.getElementById(previewId).src = e.target.result;
            }
            reader.readAsDataURL(input.files[0]);
        }
    }
</script>
